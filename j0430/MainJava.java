package j0430;

public class MainJava {
    public static void main(String[] args) {
        int myAge;    // 整数値しか保存できない、ageという名前の変数を用意する
        myAge = 30;   // 変数ageに30という整数値を代入する(保存する)
        System.out.println(myAge); // 変数ageの内容を画面に表示する

    }
}
